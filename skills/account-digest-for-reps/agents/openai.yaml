interface:
  display_name: "Account Digest For Reps"
  short_description: "Rank account signals into Slack digests"
  default_prompt: "Use $account-digest-for-reps to turn a CRM list, Salesforce report, HubSpot custom signal source, product/session activity, or CSV into a ranked Slack account digest."

policy:
  allow_implicit_invocation: true
